# Test that the Ollama server is reachable by listing available models
curl -sf "${SERVICE_BASE_URL%/}/api/tags" \
  ${UNITYSVC_API_KEY:+-H "Authorization: Bearer $UNITYSVC_API_KEY"} \
  | head -c 500

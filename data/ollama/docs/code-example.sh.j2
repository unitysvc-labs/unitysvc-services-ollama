curl "{{ service_base_url }}" \
  -H "Content-Type: application/json" \
  ${UNITYSVC_API_KEY:+-H "Authorization: Bearer $UNITYSVC_API_KEY"} \
  -d '{
    "model": "{{ routing_key.model }}}}",
    "messages": [
      {
        "role": "system",
        "content": "You are a helpful assistant."
      },
      {
        "role": "user",
        "content": "Say this is a test"
      }
    ]
  }'

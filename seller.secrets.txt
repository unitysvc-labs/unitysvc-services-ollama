# seller.secrets.txt — seller secrets required by the Ollama services
#
# Sourceable shell format — `export NAME="value"`.
#
# This repo's Ollama Cloud channel must use a real Ollama Cloud API key. Do not
# commit a real key here. Fill this value locally before uploading to a seller
# secret store, or configure OLLAMA_API_KEY as a GitHub repository secret or
# variable so the upload workflow can seed it automatically.
#
# Upload locally, after setting the value:
#     usvc_seller secrets upload seller.secrets.txt

# OLLAMA_API_KEY — REQUIRED for the `ollama-cloud` channel.
#   Used as `${ customer_secrets.OLLAMA_API_KEY }` in cloud-capable services.
export OLLAMA_API_KEY=""
